export type MapLayerType = "standard" | "satellite" | "hybrid" | "terrain";

export type NavViewMode = "follow" | "free" | "overview";

export type CameraApplyMode = "auto" | "setCamera" | "animate0" | "animate160";

export type CameraTuningPreset = "balanced" | "smooth" | "snappy";
